<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yolenith.proposito">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".MenuPomodoro"></activity>
        <activity android:name=".MenuDinero" />
        <activity android:name=".MenuPropositos" />
        <activity android:name=".VerDineroRealizado" />
        <activity android:name=".CambioDinero" />
        <activity android:name=".VerDinero" />
        <activity android:name=".AgregarDinero" />
        <activity android:name=".VerDiarioRealizado" />
        <activity android:name=".MainActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".agregar" />
        <activity android:name=".ver"/>

        <activity android:name=".cambios" />

        <service android:name=".MyTestService" />

        <receiver
            android:name=".Alarma"
            android:enabled="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

    </application>


</manifest>